# Scripts

This directory contains various scripts which are used for development and testing purposes. These scripts are not part of the main application and are not included within the python package.

`scripts/ci`: Continuous Integration (CI) scripts, including those which are called by `python-semantic-release`, `uv`, and GitHub Actions.
